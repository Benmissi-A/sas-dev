
export const buy = () => {

  // !isLogged? => log in
  // how many tokens?
  // ask for confirmation

}

export const sell = () => {
  // !isLogged? => log in
  // how many tokens?
  // ask for confirmation
}

export const wallet = () => {
  /*const [transactions, setTransactions] = useState([])

  const sum = sumOfArray(transactions);
*/
}

